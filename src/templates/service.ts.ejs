import { print } from 'gluegun'
import { ResultAsync, ok, err } from 'neverthrow'

// name: '<%= props.name %>',
async function wait(ms: number) {
    return new Promise((resolve) => setTimeout(resolve, ms))
}

export async function run<%= props.name %>(input?: unknown) {
 const hasError = Boolean(input)
 if (hasError) {
  await wait(1000)
  const error = 'todo handle error <%= props.name %> '
  return err(error)
 }

 console.debug('doing')
 print.info("done")
 let out: string
 out = "Run something <%= props.name %> out"
 out = "todo handle success <%= props.name %> out"
 return ok(out)
}