import { print } from 'gluegun'

export async function getRepoUrl() {
  console.debug('todo add ability to get git repo url the fastest way possible')
  print.info('done')
  const out = 'out'
  return out
}
